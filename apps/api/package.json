{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "prisma generate && tsx watch src/server.ts",
    "dev:vercel": "vercel dev",
    "build": "tsc -b --force",
    "start": "node dist/server.js",
    "vercel-build": "cd ../../packages/crypto && pnpm install && pnpm run build && cd ../../apps/api && prisma generate && tsc -p tsconfig.json",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:deploy": "prisma migrate deploy"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^25.2.3",
    "@types/pg": "^8.16.0",
    "@vercel/node": "^5.6.3",
    "prisma": "^7.4.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@prisma/adapter-pg": "^7.4.0",
    "@prisma/client": "^7.4.0",
    "@repo/crypto": "workspace:^",
    "dotenv": "^17.2.4",
    "fastify": "^5.7.4",
    "pg": "^8.18.0",
    "zod": "^4.3.6"
  }
}