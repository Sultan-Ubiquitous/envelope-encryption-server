# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Storage Backend ('memory' or 'prisma')
STORAGE_TYPE=memory

# Cryptography
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_KEY_HEX=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Database (required only if STORAGE_TYPE=prisma)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/secure_tx?schema=public